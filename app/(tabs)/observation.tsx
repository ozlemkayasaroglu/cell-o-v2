import React from 'react';
import { View, Text } from 'react-native';

export default function ObservationScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Observation Screen (Gözlem Ekranı)</Text>
    </View>
  );
}
